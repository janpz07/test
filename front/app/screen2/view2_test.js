'use strict';

describe('myApp.screen2 module', function() {

  beforeEach(module('myApp.screen2'));

  describe('screen2 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('screen2');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});